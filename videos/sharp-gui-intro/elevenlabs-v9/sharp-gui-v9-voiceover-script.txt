Sharp GUI，把照片变成可探索的三维空间。

拖拽上传，本地推理，压缩入库。流程都留在你的机器上。

WASM 加速实时预览。旋转、缩放、聚焦、触控，Gyro 和 XR，同屏响应。

一次部署，桌面、平板、手机和头显，都能通过浏览器进入同一个空间。

导出独立 HTML，封装 SPZ 模型。不用服务器，发给别人，打开就能看。

Sharp GUI，本地优先的空间影像工作台。喜欢的话，欢迎在 GitHub 给它点一个 Star。

