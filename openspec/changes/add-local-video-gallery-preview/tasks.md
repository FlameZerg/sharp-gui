## 1. 后端媒体模型与索引

- [ ] 1.1 定义图片/视频共享的媒体扩展名、媒体类型、稳定 ID 和索引元数据字段，保持旧照片索引可兼容读取
- [ ] 1.2 扩展相册扫描逻辑，使其能发现支持的图片与视频，并按 `media_type`、时间、名称、大小排序
- [ ] 1.3 增加媒体路径解析函数，复用真实路径 root 校验，确保视频预览、封面和下载不能越过配置目录
- [ ] 1.4 为视频元数据添加可选 ffprobe 探测能力，支持 duration、width、height、codec、bitrate 字段缺失时安全降级
- [ ] 1.5 为视频 poster 添加可选 ffmpeg 抽帧缓存，缓存文件名绑定源文件 mtime/size；没有 ffmpeg 或抽帧失败时返回占位状态

## 2. 后端 API、权限与下载

- [ ] 2.1 扩展或新增相册媒体列表 API，支持 `type=all|photo|video` 筛选并返回混合媒体条目
- [ ] 2.2 新增或扩展媒体 poster、原始播放、原始下载端点，确保视频响应支持条件请求和 Range seek
- [ ] 2.3 将新增媒体端点纳入 `backend/security/access_control.py` 权限矩阵，并保留 owner-only 配置修改规则
- [ ] 2.4 调整批量下载逻辑以支持视频文件，同时避免前端下载大视频时必须 fetch 成 blob
- [ ] 2.5 确保照片转 3D 端点继续只接受图片媒体，视频 ID 请求应返回明确失败项

## 3. 前端类型、API 与状态

- [ ] 3.1 将 `PhotoItem`/照片列表响应演进为可表达图片和视频的媒体类型，包含视频元数据、poster、playback、download 字段
- [ ] 3.2 更新 `frontend/src/api/photoGallery.ts`，为媒体筛选、视频 poster、播放和下载 URL 提供类型安全调用或 URL 构造
- [ ] 3.3 更新 Zustand 状态，增加媒体类型筛选和媒体预览状态，同时保留当前照片相册选择与照片转 3D 状态
- [ ] 3.4 调整选择逻辑，使选中项包含视频时下载可用、转 3D 只对图片可用，并在 UI 上给出清晰状态

## 4. 图库浏览 UI

- [ ] 4.1 在图库工具栏新增全部、照片、视频分段筛选控件，使用本项目 Apple 毛玻璃视觉和可访问语义
- [ ] 4.2 扩展瀑布流卡片，视频卡展示 poster、播放图标、时长、分辨率/规格标签和文件名
- [ ] 4.3 调整空状态、总数、副标题和加载状态，使其随媒体类型筛选变化
- [ ] 4.4 保持照片卡片原有预览、选择、下载、转 3D 行为，并隐藏视频卡上的转 3D 快捷入口
- [ ] 4.5 优化移动端图库布局，确保筛选、排序、密度、上传/选择等控件不溢出且触控可用

## 5. 视频预览器 UI 与交互

- [ ] 5.1 将现有图片预览器演进为媒体预览器或新增视频预览器，图片预览行为保持不变
- [ ] 5.2 使用原生 `<video>` 作为播放内核，并实现自定义玻璃风控制层：播放/暂停、进度、时间、音量/静音、全屏、下载、关闭
- [ ] 5.3 仅在移动端实现长按视频画面精细拖动 seek：区分点击播放/暂停与长按横向拖动 scrub，拖动时显示目标时间或偏移
- [ ] 5.4 支持当前相册媒体上下切换，并在视频与图片之间切换时正确释放播放状态和对象引用
- [ ] 5.5 增加视频播放错误、元数据缺失、poster 缺失的美观降级 UI；播放失败时提示下载到本地播放，并保留下载和关闭操作
- [ ] 5.6 完成 PC 与移动端预览布局，确保控件不遮挡核心画面、触控目标足够大、键盘焦点可见

## 6. 国际化、视觉稿与设计校准

- [ ] 6.1 同步维护 `frontend/src/i18n/en.json` 与 `frontend/src/i18n/zh.json` 的媒体筛选、视频控制、错误、元数据和下载文案
- [ ] 6.2 根据生成的 UI 设计图校准图库浏览、视频卡片、PC 视频预览和移动端视频预览样式
- [ ] 6.3 检查 CSS 色彩与组件形态，保持与现有 Apple 毛玻璃界面一致且避免文本溢出

## 7. 测试与验证

- [ ] 7.1 增加后端服务测试，覆盖视频扫描、路径安全、索引兼容、metadata/poster 降级和视频不参与转 3D
- [ ] 7.2 增加 API/路由/权限测试，覆盖媒体筛选、视频播放/下载端点、Range/conditional 响应和非 owner 修改限制
- [ ] 7.3 增加前端类型与交互验证，覆盖筛选、视频卡展示、选择栏动作可用性和媒体预览状态切换
- [ ] 7.4 运行后端 pytest、前端 lint 和 build，并修复新增问题
- [ ] 7.5 手动回归旧照片图库：添加相册、浏览、预览、下载、批量选择、照片转 3D 均保持可用
- [ ] 7.6 手动验证视频关键路径：MP4/MOV/WebM 浏览、poster、播放、进度条 seek、移动端长按画面拖动 scrub、下载、移动端布局
