## 1. 后端索引结构与迁移

- [x] 1.1 在 `backend/paths.py` 增加图库 catalog 与按相册索引目录等新缓存路径
- [x] 1.2 在 `backend/services/photo_gallery.py` 中抽出索引读写辅助函数，支持原子写入（临时文件 + `os.replace`）、内容未变不写盘和 JSON 读取容错
- [x] 1.3 设计并实现可解析 media_id（`<album_id>_<hash>`）以及 `catalog.json`、`albums/<album_id>.json` 的数据结构与 `index_version` 字段，确保正常读写路径上无全局可变文件（不再有 media-lookup.json / 全局 index.json）
- [x] 1.4 实现升级后的运行时一次性迁移（每 workspace 生效，非常驻脚本）：首次需要相册列表时从旧 `index.json` 一次性折算 catalog（仅计数/封面，不读媒体文件）并**立即归档为 `index.json.bak`**；`build_catalog_from_legacy_index` 在旧索引不存在时早返回，列表每次只折算一次 catalog 且不再回退重建（避免 O(N²)）。每相册索引按需在首次进入相册时扫描建立；归档前完成的首扫可复用旧元数据跳过 ffprobe，归档后直接重建（可接受），不为跨相册复用做兜底；迁移不删除原始媒体
- [x] 1.5 增加进程内相册索引缓存，基于文件 `index_version` 或 mtime 复用已解析的相册媒体列表，并提供读时快照供分页/排序迭代
- [x] 1.6 将全局 `photo_index_lock` 使用收敛为相册级锁和短生命周期 catalog 写锁，避免长扫描阻塞其它相册读取

## 2. 后端缓存优先读取与扫描语义

- [x] 2.1 重构相册摘要构建逻辑，使 `/api/photo-albums` 从 catalog 和相册配置返回轻量响应，不扫描所有相册
- [x] 2.2 重构 `list_album_photos`，使普通分页、筛选、排序从单相册索引的读时快照读取并在内存中过滤、排序、分页，后台刷新不重排当前浏览会话
- [x] 2.3 为无索引相册返回明确的待索引/扫描状态和空列表响应，避免阻塞普通列表请求
- [x] 2.4 实现全新相册的低优先级后台 bootstrap，进入图库视图后逐个填充 catalog 摘要，且可被前台进入相册/显式刷新打断让路
- [x] 2.5 保留显式 rescan 路径，让 owner 手动刷新相册时扫描目录并更新相册索引与 catalog
- [x] 2.6 优化扫描循环中的路径处理，避免在每个媒体文件上重复计算相册 root 的 `realpath`
- [x] 2.7 确保媒体计数、封面、更新时间、图片/视频筛选计数都来自缓存索引并在扫描后正确更新

## 3. 媒体解析、缩略图与缓存管理

- [x] 3.1 更新 `resolve_media_path`、`resolve_photo_path`、视频解析、下载、缩略图、poster 和照片转 3D 逻辑，使其通过 media_id 内嵌的 album_id 直接定位相册索引，无需全局查找表
- [x] 3.2 保留单文件访问的 root 校验、存在性校验、mtime/size 失效判断和非 ASCII 文件名兼容行为
- [x] 3.3 为图片缩略图生成增加并发限制，避免多个大图同时解码造成 CPU、内存或磁盘 IO 峰值
- [x] 3.4 确认视频 poster 继续按需生成并复用源文件版本缓存，不引入视频转码或代理缓存
- [x] 3.5 增加图库缓存统计服务，统计索引、缩略图、video poster 和临时下载 ZIP 的文件数量与体积
- [x] 3.6 增加 owner-only 缓存清理服务，支持清理生成缓存或指定范围缓存，且不得删除用户原始照片/视频
- [x] 3.7 保持删除相册时清理对应媒体 ID 的缩略图和 video poster 缓存，并适配新索引结构

## 4. 后端路由、权限与 API 契约

- [x] 4.1 更新 `/api/photo-albums`、`/api/photo-albums/<album_id>/photos`、`/api/photo-albums/<album_id>/scan` 的响应字段和状态语义
- [x] 4.2 新增或扩展图库缓存状态与清理 API，并统一返回 JSON
- [x] 4.3 在 `backend/security/access_control.py` 中为新增缓存清理或扫描控制接口添加 owner-only 权限规则
- [x] 4.4 确保远程已解锁用户仍可浏览、预览和下载已有缓存索引中的媒体，但不能执行 owner-only 管理动作
- [x] 4.5 更新后端错误响应，使索引缺失、相册不可用、媒体已删除、缓存清理失败等状态可被前端区分

## 5. 前端启动流程与图库状态

- [x] 5.1 调整 `frontend/src/App.tsx` 的启动流程，使 boot 不再等待 `fetchPhotoAlbums()` 完成
- [x] 5.2 在进入图库视图或图库侧栏需要展示相册时按需加载相册摘要，并避免重复请求
- [x] 5.3 更新 `frontend/src/api/photoGallery.ts` 和 `frontend/src/types/photoGallery.ts`，适配新增扫描状态、缓存状态、缓存统计和清理响应
- [x] 5.4 更新 Zustand 图库状态，区分相册摘要加载、相册内容加载、相册索引中、刷新中和缓存管理状态
- [x] 5.5 调整 `PhotoAlbumList`，在无缓存、索引中、缓存旧、路径不可用等状态下显示符合项目风格的状态反馈
- [x] 5.6 调整 `PhotoGalleryView`，使打开有缓存的大相册先显示缓存第一页，显式刷新才触发相册扫描
- [x] 5.7 保持排序、筛选、滚动加载更多、选择、下载、视频预览和照片转 3D 的现有交互不退化

## 6. 缓存管理 UI 与国际化

- [x] 6.1 在合适的 owner-only 设置或图库管理位置增加图库缓存状态展示与清理入口，保持 Apple 毛玻璃风格
- [x] 6.2 为缓存清理动作增加确认、进行中、成功、失败和不可用状态，避免误导用户删除原始媒体
- [x] 6.3 同步维护 `frontend/src/i18n/zh.json` 和 `frontend/src/i18n/en.json` 的新增用户可见文案
- [x] 6.4 确保新增缓存管理控件具备清晰 aria-label、键盘焦点和移动端可用布局

## 7. 测试与性能验证

- [x] 7.1 增加后端服务测试，覆盖一次性迁移+立即归档、归档前首扫复用旧元数据（零 ffprobe）、迁移后多次列表不再重建旧索引（防 O(N²)）、新索引读写、内容未变不写盘、media_id 解析和相册级缓存读取
- [x] 7.2 增加后端路由测试，覆盖相册列表不扫描所有相册、分页排序不触发重扫、显式 rescan 更新索引、后台刷新不重排当前浏览快照
- [x] 7.3 增加安全测试，覆盖 media_id root 校验、旧缓存指向已删除文件、非 owner 缓存清理拒绝和远程浏览权限
- [x] 7.4 增加缓存管理测试，覆盖缓存统计、清理索引/缩略图/poster/临时 ZIP、不删除原始媒体
- [x] 7.5 增加前端测试或手动验证记录，覆盖启动不等待图库、进入图库按需加载、无缓存相册状态、有缓存分页排序体验
- [x] 7.6 构造至少 1000+ 媒体条目的相册，验证 Success Metrics：启动时间与图库规模解耦且零目录扫描、暖索引第一页交互级可用、翻页/切排序/切筛选零 `os.walk`、扫描一个相册不阻塞其它相册（已用自动化暖索引测试覆盖核心零扫描指标；真实设备/NAS 体感建议验收时复测）
- [x] 7.7 回归图片和视频关键路径：缩略图、video poster、原图/视频预览、下载、批量下载、照片转 3D、删除相册缓存清理
- [x] 7.8 运行后端 pytest、前端 lint 和 build，并把前端 build 产物按项目规范纳入最终变更
