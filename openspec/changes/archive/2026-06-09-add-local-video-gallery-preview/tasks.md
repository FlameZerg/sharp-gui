## 1. 后端媒体模型与索引

- [x] 1.1 定义图片/视频共享的媒体扩展名、媒体类型、稳定 ID 和索引元数据字段，保持旧照片索引可兼容读取
- [x] 1.2 扩展相册扫描逻辑，使其能发现支持的图片与视频，并按 `media_type`、时间、名称、大小排序
- [x] 1.3 增加媒体路径解析函数，复用真实路径 root 校验，确保视频预览、封面和下载不能越过配置目录
- [x] 1.4 为视频元数据添加可选 ffprobe 探测能力，支持 duration、width、height、codec、bitrate 字段缺失时安全降级
- [x] 1.5 为视频 poster 添加可选 ffmpeg 抽帧缓存，缓存文件名绑定源文件 mtime/size；没有 ffmpeg 或抽帧失败时返回占位状态

## 2. 后端 API、权限与下载

- [x] 2.1 扩展或新增相册媒体列表 API，支持 `type=all|photo|video` 筛选并返回混合媒体条目
- [x] 2.2 新增或扩展媒体 poster、原始播放、原始下载端点，确保视频响应支持条件请求和 Range seek
- [x] 2.3 将新增媒体端点纳入 `backend/security/access_control.py` 权限矩阵，并保留 owner-only 配置修改规则
- [x] 2.4 调整批量下载逻辑以支持视频文件，同时避免前端下载大视频时必须 fetch 成 blob
- [x] 2.5 确保照片转 3D 端点继续只接受图片媒体，视频 ID 请求应返回明确失败项
- [x] 2.6 修复非 ASCII 视频文件名的响应头编码问题，避免手写 `Content-Disposition` 导致中文文件名预览 500
- [x] 2.7 删除相册时清理该相册媒体 ID 对应的图片缩略图和视频 poster 缓存
- [x] 2.8 为批量下载临时 ZIP 增加过期清理兜底，避免中断下载遗留 `photo-gallery-*.zip`
- [x] 2.9 增加短期签名路径式视频播放 URL `/api/video-play/<video_id>/<play_token>/<filename>`，降低移动端原生播放器接管时丢失 Cookie/query 的风险
- [x] 2.10 确保播放 token 只授权 inline 播放，不绕过 `/api/video-original/<id>?download=1` 的 Unlocked 下载权限

## 3. 前端类型、API 与状态

- [x] 3.1 将 `PhotoItem`/照片列表响应演进为可表达图片和视频的媒体类型，包含视频元数据、poster、playback、download 字段
- [x] 3.2 更新 `frontend/src/api/photoGallery.ts`，为媒体筛选、视频 poster、播放和下载 URL 提供类型安全调用或 URL 构造
- [x] 3.3 更新 Zustand 状态，增加媒体类型筛选和媒体预览状态，同时保留当前照片相册选择与照片转 3D 状态
- [x] 3.4 调整选择逻辑，使选中项包含视频时下载可用、转 3D 只对图片可用，并在 UI 上给出清晰状态

## 4. 图库浏览 UI

- [x] 4.1 在图库工具栏新增全部、照片、视频分段筛选控件，使用本项目 Apple 毛玻璃视觉和可访问语义
- [x] 4.2 扩展瀑布流卡片，视频卡展示 poster、播放图标、时长、分辨率/规格标签和文件名
- [x] 4.3 调整空状态、总数、副标题和加载状态，使其随媒体类型筛选变化
- [x] 4.4 保持照片卡片原有预览、选择、下载、转 3D 行为，并隐藏视频卡上的转 3D 快捷入口
- [x] 4.5 优化移动端图库布局，确保筛选、排序、密度、上传/选择等控件不溢出且触控可用

## 5. 视频预览器 UI 与交互

- [x] 5.1 将现有图片预览器演进为媒体预览器或新增视频预览器，图片预览行为保持不变
- [x] 5.2 使用原生 `<video>` 作为播放内核，并实现自定义玻璃风控制层：播放/暂停、进度、时间、音量/静音、全屏、下载、关闭
- [x] 5.3 仅在移动端实现长按视频画面精细拖动 seek：区分点击播放/暂停与长按横向拖动 scrub，拖动时显示目标时间或偏移
- [x] 5.4 支持当前相册媒体上下切换，并在视频与图片之间切换时正确释放播放状态和对象引用
- [x] 5.5 增加视频播放错误、元数据缺失、poster 缺失的美观降级 UI；播放失败时提示下载到本地播放，并保留下载和关闭操作
- [x] 5.6 完成 PC 与移动端预览布局，确保控件不遮挡核心画面、触控目标足够大、键盘焦点可见
- [x] 5.7 将预览器视频单文件下载改为 fetch/blob 保存路径，规避 Chrome 在预览浮层中直链下载大视频失败的问题
- [x] 5.8 为移动端 `<video>` 增加内联播放兼容属性，并记录安卓浏览器强制接管时只能尽量兼容、失败降级的边界
- [x] 5.9 横屏视频在移动端全屏时尝试锁定 landscape，失败时静默降级并保持播放可用
- [x] 5.10 优化移动端控制栏折叠/展开：点击非按钮区域仅展开控制栏，不强制回到顶部
- [x] 5.11 根据实际验收调整播放器控制栏：播放/seek 居中、音量靠左、下载/全屏靠右，小巧悬浮并支持自动隐藏
- [x] 5.12 为全屏/退出全屏提供不同图标状态，避免用户无法判断当前动作

## 6. 国际化、视觉稿与设计校准

- [x] 6.1 同步维护 `frontend/src/i18n/en.json` 与 `frontend/src/i18n/zh.json` 的媒体筛选、视频控制、错误、元数据和下载文案
- [x] 6.2 根据生成的 UI 设计图校准图库浏览、视频卡片、PC 视频预览和移动端视频预览样式
- [x] 6.3 检查 CSS 色彩与组件形态，保持与现有 Apple 毛玻璃界面一致且避免文本溢出
- [x] 6.4 将侧栏入口从“照片”调整为更通用的“图库”，匹配图片与视频混合媒体能力
- [x] 6.5 校准类型筛选控件为共享背景的分段滑块样式，去掉割裂的数字椭圆徽标并保持工具栏控件尺寸一致
- [x] 6.6 重做视频播放失败态视觉，使用项目玻璃态卡片和按钮，避免突兀的大胶囊按钮
- [x] 6.7 优化中间宽度、平板竖屏和手机竖屏布局，确保按钮不截断、文字不竖排、文件名不遮挡控制栏
- [x] 6.8 优化移动端图库工具栏状态切换性能：使用稳定占位高度隔离瀑布流布局，展开/折叠只动画内容层，避免列表跟随上下抖动
- [x] 6.9 修复移动端工具栏毛玻璃退化问题：避免在外层占位节点创建 backdrop root，保留与 PC 端同源的 Apple 玻璃质感
- [x] 6.10 校准移动端工具栏展开态信息密度：收紧标题块与类型筛选间距，提高副标题/统计文案可读性但保持弱提示层级
- [x] 6.11 校准移动端工具栏动效曲线：避免面板与子元素同时过冲，只保留整块玻璃面板的轻微吸附回弹

## 7. 测试与验证

- [x] 7.1 增加后端服务测试，覆盖视频扫描、路径安全、索引兼容、metadata/poster 降级和视频不参与转 3D
- [x] 7.2 增加 API/路由/权限测试，覆盖媒体筛选、视频播放/下载端点、Range/conditional 响应和非 owner 修改限制
- [x] 7.3 增加前端类型与交互验证，覆盖筛选、视频卡展示、选择栏动作可用性和媒体预览状态切换
- [x] 7.4 运行后端 pytest、前端 lint 和 build，并修复新增问题
- [x] 7.5 手动回归旧照片图库：添加相册、浏览、预览、下载、批量选择、照片转 3D 均保持可用
- [x] 7.6 手动验证视频关键路径：MP4/MOV/WebM 浏览、poster、播放、进度条 seek、移动端长按画面拖动 scrub、下载、移动端布局
- [x] 7.7 增加回归测试覆盖中文视频文件名、预览下载、删除相册缓存清理和批量 ZIP 过期清理
- [x] 7.8 验证路径式播放 token 的成功、过期、撤销会话失效和不能绕过下载权限
- [x] 7.9 手动记录移动端浏览器兼容边界：Chrome 使用网页播放器表现稳定，夸克接管后可播放，小米浏览器接管后仍可能失败并走下载降级
- [x] 7.10 手动验证移动端工具栏真实设备体验：折叠/展开动画保留轻微吸附回弹，不破坏毛玻璃采样，不推动瀑布流列表位移
- [x] 7.11 手动验证移动端工具栏展开态：副标题/统计文案在真实手机上可读，标题、说明和类型筛选之间没有明显空白
