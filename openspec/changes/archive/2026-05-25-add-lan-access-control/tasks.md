## 1. 基础配置与权限模型

- [x] 1.1 定义 `access_control` 配置结构，兼容旧 `config.json` 缺省字段。
- [x] 1.2 实现访问码哈希、会话密钥、会话版本和会话有效期的读写逻辑。
- [x] 1.3 定义后端权限层级：Public、Unlocked、Owner、Conditional。
- [x] 1.4 梳理现有所有 `/api/*`、`/files/*`、模型资源和照片资源端点，建立权限分类表。

## 2. 后端认证与请求拦截

- [x] 2.1 新增认证状态、登录、登出、设置访问码、撤销会话和访问控制设置 API。
- [x] 2.2 实现 HttpOnly Cookie 会话签发与校验，包含过期时间和 `session_version` 校验。
- [x] 2.3 在 Flask `before_request` 中统一拦截未授权私有 API 和 `/files/*` 请求。
- [x] 2.4 为登录失败增加轻量限速或递增延迟，降低弱访问码被暴力猜测的风险。
- [x] 2.5 保持 localhost owner 免登录，并确保 owner-only 操作不会被远程访问码提升权限。
- [x] 2.6 增加 Host 允许列表与真实来源地址校验，避免 DNS rebinding 或伪造转发头绕过 owner 判断。
- [x] 2.7 将 `/api/generate` 与 `/api/photo-conversions` 接入 `allow_remote_generation` 条件权限。
- [x] 2.8 调整 CORS 响应，移除与 Cookie 门禁冲突的通配开放行为。
- [x] 2.9 确保静态 SPA 壳、assets、favicon、manifest 和认证 API 在未授权时仍可访问。

## 3. 前端 API、状态与门禁页

- [x] 3.1 新增 `frontend/src/api/auth.ts` 并从 `api/index.ts` 导出认证相关 API。
- [x] 3.2 扩展 `frontend/src/types/`，新增认证状态、登录请求、访问控制设置类型。
- [x] 3.3 扩展 Zustand store，记录认证状态、owner 状态、setup required 和权限不足状态。
- [x] 3.4 调整 `frontend/src/api/client.ts`，统一识别 401/403 并保留原有错误数据。
- [x] 3.5 调整 `App.tsx` 启动流程：先获取认证状态，再决定展示门禁页或加载现有数据。
- [x] 3.6 新增门禁页组件三件套，支持访问码输入、登录中状态、错误提示和键盘提交。

## 4. 设置页与用户体验

- [x] 4.1 在 Settings 中新增“局域网门禁”配置区，展示启用状态、owner 状态和安全提示。
- [x] 4.2 支持 owner 设置或修改访问码，保存后不暴露明文。
- [x] 4.3 支持 owner 设置会话有效期、远程生成开关和撤销所有设备。
- [x] 4.4 支持远程已解锁设备登出当前会话。
- [x] 4.5 对 403 owner-only 错误显示“仅本机可执行”的可理解反馈。
- [x] 4.6 若局域网访问开启但未设置访问码，在本机设置页给出明确提醒。
- [x] 4.7 支持 owner 关闭局域网门禁，并在关闭时收起访问码、会话和远程生成设置，仅保留开关与风险提示。
- [x] 4.8 启动提醒支持“稍后”和“不再提示”：稍后只关闭本次提示，不再提示持久抑制。

## 5. 私有资源保护回归

- [x] 5.1 验证模型列表、模型缩略图、模型原图、模型文件、下载和导出在未授权远程请求下均被拒绝。
- [x] 5.2 验证照片相册列表、照片列表、照片缩略图、原图、批量下载在未授权远程请求下均被拒绝。
- [x] 5.3 验证 `/files/*` 工作区资源在未授权远程请求下均被拒绝。
- [x] 5.4 验证远程已解锁设备可以正常浏览、预览和下载模型/照片。
- [x] 5.5 验证远程已解锁设备默认不能执行设置、目录管理、删除、重启、取消任务和生成任务。
- [x] 5.6 验证开启 `allow_remote_generation` 后远程生成可用，owner-only 操作仍被拒绝。
- [x] 5.7 验证伪造 `X-Forwarded-For`、`X-Real-IP` 或异常 Host 不能获得 owner 权限。
- [x] 5.8 验证连续错误访问码会被限速或延迟，正确访问码在限速窗口结束后仍可登录。
- [x] 5.9 验证门禁关闭时远程可浏览读取资源，但 owner-only 管理端点仍被拒绝。

## 6. 国际化、文档与验证

- [x] 6.1 在 `frontend/src/i18n/en.json` 与 `frontend/src/i18n/zh.json` 同步新增全部门禁文案。
- [x] 6.2 更新 README / README.en，说明局域网访问码、HTTPS 建议、localhost owner 和远程权限边界。
- [x] 6.3 更新 Agent 规则或后端指南中的安全端点清单，记录新的权限分级。
- [x] 6.4 运行前端构建和可用 lint 检查，确认新增类型与 i18n 无遗漏。
- [x] 6.5 运行后端语法检查或 smoke test，覆盖认证 API、受保护资源和 owner-only API。
- [x] 6.6 手动验证本机、同局域网手机/平板、会话过期、撤销会话、修改访问码后的行为。
